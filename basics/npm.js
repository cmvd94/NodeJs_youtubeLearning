/*  //node package manager.

const { default: nodemon } = require("nodemon")

//global package
npm i nodemon -g//Monitor for any changes in your node.js application and automatically restart the server - perfect for development
//local (aplplication for our project)
npm init or npm init -y //create package.json

//we can install package for this application . and if we upload in github we should negelect package files for unwanted storage
//once it is installed ,new json file is installed and in our application file we have dependencies in which newly installed packed is linked

//instead of global dependencies we can declare development dependencies
npm i nodemon -D //change in package //should write in package   inside scipt // "dev" : "nodemon index" 

//remove
npm rm/unistall nodemon -D

*/